<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Subscription Renewed Successfully</title>
  <style>
    :root {
      --color-primary: #fd8500;
      --color-primary-light: #ffa033;
      --color-primary-dark: #d3720b;
      --color-secondary: #164589;
      --color-secondary-light: #2a5ca6;
      --color-secondary-dark: #0e3266;
      --color-gray-100: #f8f9fa;
      --color-gray-200: #e9ecef;
      --color-gray-300: #dee2e6;
      --color-gray-600: #6c757d;
      --color-gray-900: #212529;
      --spacing-xs: 0.5rem;
      --spacing-sm: 1rem;
      --spacing-md: 1.5rem;
      --spacing-lg: 2rem;
      --border-radius: 8px;
      --box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Roboto, -apple-system, BlinkMacSystemFont, sans-serif;
      line-height: 1.6;
      color: var(--color-gray-900);
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
    }

    .container {
      box-shadow: var(--box-shadow);
      border-radius: var(--border-radius);
      overflow: hidden;
      margin: var(--spacing-md);
    }

    .header {
      background-color: var(--color-secondary);
      color: white;
      text-align: center;
      padding: var(--spacing-md) 0;
    }

    .logo {
      max-width: 120px;
      height: auto;
      color: white;
    }

    .content {
      padding: var(--spacing-lg);
    }

    h1 {
      color: var(--color-secondary);
      margin-bottom: var(--spacing-md);
      font-weight: 600;
    }

    p {
      margin-bottom: var(--spacing-sm);
    }

    .subscription-details {
      background-color: var(--color-gray-100);
      padding: var(--spacing-md);
      border-radius: var(--border-radius);
      margin: var(--spacing-md) 0;
      border-left: 4px solid var(--color-secondary);
    }

    .subscription-details h3 {
      color: var(--color-secondary);
      margin-bottom: var(--spacing-sm);
    }

    .detail-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: var(--spacing-xs);
      padding-bottom: var(--spacing-xs);
      border-bottom: 1px solid var(--color-gray-300);
    }

    .detail-row:last-child {
      border-bottom: none;
    }

    .detail-label {
      font-weight: 500;
      color: var(--color-gray-600);
    }

    .detail-value {
      font-weight: 500;
    }

    .button {
      display: inline-block;
      background-color: var(--color-primary);
      color: white;
      text-decoration: none;
      padding: var(--spacing-sm) var(--spacing-lg);
      border-radius: var(--border-radius);
      margin: var(--spacing-sm) 0;
      font-weight: 500;
      text-align: center;
      transition: background-color 0.3s ease;
    }

    .button:hover {
      background-color: var(--color-primary-dark);
    }

    .cta-container {
      text-align: center;
      margin: var(--spacing-md) 0;
    }

    a {
      color: var(--color-secondary);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
      color: var(--color-secondary-dark);
    }

    .footer {
      font-size: 0.875rem;
      color: var(--color-gray-600);
      text-align: center;
      padding: var(--spacing-md);
      background-color: var(--color-gray-100);
    }

    @media screen and (max-width: 480px) {
      .container {
        margin: var(--spacing-xs);
      }

      .content {
        padding: var(--spacing-md);
      }

      .detail-row {
        flex-direction: column;
      }

      .detail-value {
        margin-top: 0.25rem;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="header">
      <img src="<%= logoUrl %>" alt="<%= appName %> Logo" class="logo">
    </div>
    <div class="content">
      <h1>Subscription Renewed Successfully</h1>
      <p>Hello <%= user.firstName %>,</p>
      <p>Great news! Your subscription to <strong>
          <%= appName %>
        </strong> has been successfully renewed.</p>

      <div class="subscription-details">
        <h3>Updated Subscription Details:</h3>
        <div class="detail-row">
          <span class="detail-label">Phone Number:</span>
          <span class="detail-value">
            <%= subscription.phoneNumber %>
          </span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Plan:</span>
          <span class="detail-value">
            <%= subscription.plan %>
          </span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Price:</span>
          <span class="detail-value">$<%= subscription.price %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Start Date:</span>
          <span class="detail-value">
            <%= subscription.startDate %>
          </span>
        </div>
        <div class="detail-row">
          <span class="detail-label">End Date:</span>
          <span class="detail-value">
            <%= subscription.endDate %>
          </span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Auto Renewal:</span>
          <span class="detail-value">
            <%= subscription.autoRenew ? 'Enabled' : 'Disabled' %>
          </span>
        </div>
      </div>

      <p>You can view and manage your subscription at any time through your dashboard:</p>

      <div class="cta-container">
        <a href="<%= dashboardUrl %>" class="button">View Dashboard</a>
      </div>

      <p>If you have any questions about your subscription, please contact our support team at <a
          href="mailto:<%= supportEmail %>">
          <%= supportEmail %>
        </a>.</p>

      <p>Thank you for your continued trust in <strong>
          <%= appName %>
        </strong>!</p>

      <p>Best regards,<br>The <%= appName %> Team</p>
    </div>

    <div class="footer">
      <p>&copy; <%= new Date().getFullYear() %>
          <%= appName %>. All rights reserved.</p>
    </div>
  </div>
</body>

</html>