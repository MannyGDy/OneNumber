<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to <%= appName %>
  </title>
  <style>
    :root {
      --color-primary: #fd8500;
      --color-primary-light: #ffa033;
      --color-primary-dark: #d3720b;
      --color-secondary: #164589;
      --color-secondary-light: #2a5ca6;
      --color-secondary-dark: #0e3266;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      background-color: #f8f9fa;
    }

    .email-container {
      background-color: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
      margin: 20px 10px;
    }

    .header {
      text-align: center;
      padding: 30px 0;
      background-color: var(--color-secondary);
    }

    .logo {
      max-width: 180px;
      height: auto;
    }

    .content {
      padding: 30px 25px;
    }

    h1 {
      color: var(--color-secondary);
      margin-bottom: 24px;
      font-weight: 600;
      font-size: 24px;
    }

    p {
      margin-bottom: 16px;
      color: #4a4a4a;
      font-size: 16px;
    }

    .highlight {
      background-color: #f8f9fa;
      border-left: 4px solid var(--color-primary);
      padding: 12px 16px;
      margin: 20px 0;
      border-radius: 0 4px 4px 0;
    }

    .button {
      display: inline-block;
      background-color: var(--color-primary);
      color: white;
      text-decoration: none;
      padding: 12px 24px;
      border-radius: 6px;
      margin: 20px 0;
      font-weight: 500;
      text-align: center;
      transition: background-color 0.2s ease;
    }

    .button:hover {
      background-color: var(--color-primary-dark);
    }

    .divider {
      height: 1px;
      background-color: #eee;
      margin: 24px 0;
    }

    .footer {
      font-size: 13px;
      color: #777;
      text-align: center;
      padding: 20px;
      background-color: #f8f9fa;
    }

    .social-links {
      margin: 15px 0;
    }

    .social-icon {
      display: inline-block;
      width: 32px;
      height: 32px;
      background-color: var(--color-secondary);
      border-radius: 50%;
      color: white;
      text-align: center;
      line-height: 32px;
      margin: 0 5px;
      font-size: 16px;
    }

    @media only screen and (max-width: 480px) {
      .content {
        padding: 20px 15px;
      }

      h1 {
        font-size: 22px;
      }
    }
  </style>
</head>

<body>
  <div class="email-container">
    <div class="header">
      <img src="<%= logoUrl %>" alt="<%= appName %> Logo" class="logo">
    </div>
    <div class="content">
      <h1>Welcome to <%= appName %>!</h1>
      <p>Hello <%= user.firstName %>,</p>
      <p>Thank you for joining <%= appName %>. We're excited to have you on board!</p>

      <div class="highlight">
        <p style="margin-bottom: 0;">Your account has been successfully created with email: <strong>
            <%= user.email %>
          </strong></p>
      </div>

      <p>You can now access your account and explore all our features:</p>

      <a href="<%= loginUrl %>" class="button">Login to Your Account</a>

      <p>If you have any questions or need assistance, please don't hesitate to contact our support team at <a
          href="mailto:<%= supportEmail %>" style="color: var(--color-primary);">
          <%= supportEmail %>
        </a>.</p>

      <div class="divider"></div>

      <p>Best regards,<br><strong>The <%= appName %> Team</strong></p>
    </div>
    <div class="footer">
      <p>&copy; <%= new Date().getFullYear() %>
          <%= appName %>. All rights reserved.</p>
      <p>This email was sent to <%= user.email %>.</p>
    </div>
  </div>
</body>

</html>